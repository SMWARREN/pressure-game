/**
 * Grid size constants for game board configuration
 */

/** Minimum grid size supported (in tiles per side for square grids) */
export const GRID_SIZE_MIN = 4;

/** Maximum grid size supported (in tiles per side for square grids) */
export const GRID_SIZE_MAX = 10;
