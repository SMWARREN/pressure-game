/**
 * Timing constants for game animations and state management
 */

/** Duration (ms) for clearing the justRotated flag after a tile rotation animation */
export const UNDO_DELAY_MS = 300;

/** Duration (ms) for clearing the "new tile" glow display state */
export const HISTORY_TRIM_DELAY_MS = 1500;

/** Duration (ms) for the screen shake effect when losing a level */
export const SCREEN_SHAKE_DURATION_MS = 400;
